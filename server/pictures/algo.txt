B entier
C entier

debut

C <- Modulo(B, 2) 

SI C <> 1 Alors
	Ecrire(C'est pair)
Sinon
	Ecrire(C'est impair)
fin



max entier
min entier
tab(10) entier
somme entier

debut
Pour i de 0 à 9 pas de 1
  Lire(A)
   tab[i] <- A
fin pour

pour i de 0 à 9 pas de 1
   somme <- somme + tab[i]
	si (tab[i] > min) alors
		min <- tab[i]
	finSI
finpour

pour i de 0 à 9 pas de 1
	si ( tab[i] > max) alors
		max <- tab[i]
	finSi
	
	si (tab[i] < min) alors
		min <- tab[i]
	finSi
finpour
fin

n entier
r entier

pour i de 0 à n pas de 1
	si r <> n alor
		r <- r+i
	finsi


ha entier
ma entier
hd entier
md entier
duree entier
depart entier
arrive entier

debut

lire(hd, md, ha, ma)

depart <- hd * 60 + md
arrive <- ha * 60 + ma

si arrive > depart alors
	duree <- arrive - depart

sinon
	duree <- depart - arrive
finsi

Si duree / 60 >= 24 alors
	ecrire(c'est pas possible)
finsi

fin


jours, mois entier

debut

fin
	